import PagePlaceholder from '../components/PagePlaceholder'

// Also mounted at /category/:slug (Paso 4) — same listing component, category comes from the route param.
export default function Shop() {
  return <PagePlaceholder name="Shop" />
}
