#-------------------------------------------------------------------------------#
#               Qodana analysis is configured by qodana.yaml file               #
#             https://www.jetbrains.com/help/qodana/qodana-yaml.html            #
#-------------------------------------------------------------------------------#

#################################################################################
#              WARNING: Do not store sensitive information in this file,        #
#               as its contents will be included in the Qodana report.          #
#################################################################################
version: "1.0"

#Specify inspection profile for code analysis
profile:
  name: qodana.starter

#Enable inspections
#include:
#  - name: <SomeEnabledInspectionId>

#Disable inspections
#exclude:
#  - name: <SomeDisabledInspectionId>
#    paths:
#      - <path/where/not/run/inspection>

projectJDK: "21" #(Applied in CI/CD pipeline)

#Execute shell command before Qodana execution (Applied in CI/CD pipeline)
#bootstrap: sh ./prepare-qodana.sh

#Install IDE plugins before Qodana execution (Applied in CI/CD pipeline)
#plugins:
#  - id: <plugin.id> #(plugin id can be found at https://plugins.jetbrains.com)

# Quality gate. Will fail the CI/CD pipeline if any condition is not met
# severityThresholds - configures maximum thresholds for different problem severities
# testCoverageThresholds - configures minimum code coverage on a whole project and newly added code
# dependencyLicenses - fails the run on prohibited or unknown dependency licenses
#
# B4: every failureCondition shipped commented out, so Qodana ran on each push to main and could
# never fail anything — advisory only. `critical: 0` is the part worth turning on now: a critical
# finding is not something to merge past. The `any` threshold stays commented until there is a real
# report to calibrate it against; enabling it blind would very likely paint main red for reasons
# nobody has looked at yet.
failureConditions:
  severityThresholds:
    critical: 0
#    any: 15          # descomentar con el número que salga del primer informe
# testCoverageThresholds no aplica aquí: el linter configurado abajo es qodana-jvm-community y la
# cobertura solo está disponible en los planes Ultimate. La cobertura del backend se mide con JaCoCo
# (mvn verify -> target/site/jacoco/index.html).
#  dependencyLicenses:
#    failOnProhibited: true
#    failOnUnknown: false

#Qodana supports other languages, for example, Python, JavaScript, TypeScript, Go, C#, PHP
#For all supported languages see https://www.jetbrains.com/help/qodana/linters.html
linter: jetbrains/qodana-jvm-community:2026.2
